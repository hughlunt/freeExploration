# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                         controllers.HomeController.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file             controllers.Assets.versioned(path="/public", file: Asset)

# Free Monad version
POST    /v2/invoices              controllers.InvoiceCreationController.siteInitiated
GET     /v2/invoices/:id          controllers.InvoiceRetrievalController.getSingleInvoice(id: java.util.UUID)

# Tagless final version
POST    /v3/invoices                controllers.InvoiceCreationController.taglessSiteInitiated
POST    /v3/invoices/rfi            controllers.InvoiceCreationController.taglessSponsorInitiated
GET     /v3/invoices/:id            controllers.InvoiceRetrievalController.getTaglessSingleInvoice(id: java.util.UUID)